# If frontend and backend are served from the same domain behind a reverse proxy:
VITE_API_URL="/api"

# If frontend and backend are hosted on separate domains, use the backend API URL:
# VITE_API_URL="https://api.your-domain.com/api"
