# Auto mode uses the same hostname/IP as the browser and port 5000.
# This works well for tablet/phone access over LAN.
VITE_API_URL=auto
VITE_API_PORT=5000

# Used only when VITE_API_URL starts with /api (dev proxy mode)
VITE_API_PROXY_TARGET=http://localhost:5000
