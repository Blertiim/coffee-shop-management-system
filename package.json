{
  "name": "coffee-shop-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Desktop launcher and build orchestration for the coffee shop POS.",
  "main": "electron/main.js",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm --prefix frontend run build",
    "electron": "npm run build && electron .",
    "shortcut:create": "powershell -ExecutionPolicy Bypass -File .\\scripts\\create-electron-shortcut.ps1"
  },
  "devDependencies": {
    "electron": "^33.2.1"
  }
}
